# Copy this file to .env.local and fill in your key.
# The key is read ONLY on the server (src/lib/extractInvoice.ts) and is never
# exposed to the browser. .env.local is gitignored.
ANTHROPIC_API_KEY=sk-ant-...
